static class LevelStore
{
	static field levels = {};

	static function GetLevel(name)
	{
		level = levelts.get(name);
		if (level == null)
		{
			level = new ProtoLevel(name);
			levels[name] = level;
		}
		return level.GenerateNewLevel();
	}
}
